module.exports = {
  testPathIgnorePatterns: ['<rootDir>/node_modules/'], // Exclude only the "node_modules" directory
  rules: {
    // You can add or adjust rules here to fit your project's needs
  },
};
